# 新录制自动续跑协议

2026-09-18建立。现有18日已经反复探索；此后仅对日期晚于9月17日、此前未处理的新QQQ录制继续做纸面检验。自动流程不发送任何交易订单。

冻结三条既有规则：早盘第30秒的一分钟强变动反转；同入场加正股不利0.5D止损；逐秒强变动消退后反转。均为平值单张、1秒入场延迟、严格新鲜入场、最长300秒，退出等待与成本不变。缺退出保留资金与零回收压力损失。规则参数、成本、输入清单和依赖代码哈希在启动时冻结；依赖被修改则停止处理并明确报告，不能悄悄换规则。

每30秒检查档案目录，要求原始gzip及对应哈希侧文件存在，并且文件大小和修改时间至少60秒不再变化。首次发现稳定的新录制后，在本工程构建特征。常规时段正股新鲜报价覆盖不足95%，或最后可用正股深度早于15:59:30纽约时间的录制暂不评估，等待完整数据。此覆盖门槛仅控制数据完整性，不根据盈利结果删样本。

新录制全部到齐后才做历史回放，因此不是盘中实盘执行。使用此前5个已可用日期校准，绝不使用当天未来收益决定参数。逐笔核对原始期权报价和现金恒等式，保留所有失败状态；原始数据只读，新派生数据和结果写本工程。

每个新日期保存独立报告，并更新累计按日结果。至少10个后续日期、30笔已退出交易、60%日期压力盈利、累计及省略最佳日后压力损益为正、按日平均净收益区间下界大于零时，只标记“待进一步检验候选”，不自动发布或实盘交易。三个相关候选的检验仍有多重比较问题。

未满足条件时继续等待新录制，不改变门槛或删除失败日期。用户现已确认真实费用为每张单边1.50美元，工程配置及本次冻结采用该值；单腿往返手续费3美元。若费用以后改变，需要建立新的冻结版本，不能改写既有结果。
