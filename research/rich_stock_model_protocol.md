# 更完整的正股微结构特征预测期权净收益

2026-09-18固定。使用已锁定的完整18日输入，沿用原前5个评估日选择，旧后4日、补齐9/10日、已查看16/17日分别报告，不把新补齐与重复查看日期混成新留出。

输入只用正股：先前15项价格/成交压力/时段特征，加盘口深度不平衡及变化、微价格偏离、多个窗口OFI、主动量方向代理、成交量/笔数/大小、短期收益及波动。OFI按当前最优盘口总量缩放后作带符号log1p；量、笔数、订单大小作log1p。不使用期权价格作预测输入，也不使用任何未来标签列。

两个固定模型，不调参：中位数填补和缺失指示、标准化后的Ridge(alpha=1000)；以及相同历史填补、深度3、150轮、学习率0.05、每叶至少100、L2=20的梯度提升树。填补、缩放只在此前5个完整日期拟合。每天冻结；当前日期结束后才将其已发生目标用于后续日期。

目标复用经过核对的独立买入CALL/PUT情景净收益，含价差、手续费和滑点；无法退出使用负入场资金压力标签，无法入场不填零。旧目标缓存须匹配特征与归一化文件哈希，新4日目标按同一代码生成。每个方向至少500个可用历史目标。

分别预测60/180/300秒，每次在当前有新鲜报价的方向中选择预测净收益最大的方向，门槛固定0或3美元，共2模型×3期限×2门槛=12组合。每分钟第30秒检查、严格新鲜入场、最多一张、其他成交规则沿用。模型即使前段通过，也需面对时点敏感性；先前发现的相位问题不会因换模型自动消失。

候选门槛保持至少30笔、4天有交易、3天压力盈利、累计及省略最佳日后压力盈利为正，最多3个。完整失败结果、训练日期和输入列名单都保存。此轮仍是累计探索的一部分。

费用修订：用户随后确认每张单边总手续费为1.50美元。此后重跑从工程配置读取该费用；旧缓存中已退出标签每笔再扣1.70美元，无法退出的压力标签再扣0.85美元，然后重新拟合模型和生成交易，不能只从旧组合最终收益减手续费。原0.65美元场景报告及其协议快照保留不变。
